<%= form_for(@labo) do |f| %>
  <% if @labo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@labo.errors.count, "error") %> prohibited this labo from being saved:</h2>

      <ul>
      <% @labo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :docent_id %><br />
    <%= f.text_field :docent_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
